const Route1 = () => {
  const node1 = document.createElement('div');
  const innerText = document.createTextNode('route1');
  node1.appendChild(innerText);

  return node1;
};

export default Route1;
