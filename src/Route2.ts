export const Route2 = () => {
  const node1 = document.createElement('div');
  const innerText = document.createTextNode('route2');
  node1.appendChild(innerText);

  return node1;
};

export default Route2;
